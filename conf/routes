# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

POST    /users/friends             		controllers.UserController.createFriendConnection
POST    /users/friends/list        		controllers.UserController.getUserFriends
POST    /users/friends/common      		controllers.UserController.getCommonFriends
POST    /users/subscribe/update/list    controllers.UserController.getUsersCanReceiveUpdate

PUT    /users/subscription/subscribe    controllers.UserController.subscribeToUpdate
PUT    /users/subscription/block   		controllers.UserController.blockUpdate

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
